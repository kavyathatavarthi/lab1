Common:
git config --global user.name "YourName"
git config --global user.email "your_email@example.com"
git clone https://github.com/kavyathatavarthi/basic-cli.git
cd basic-cli
git init
git status
do changes in file
git add exp1.py or git add .
git commit -m "Initial commit: Added application source code"
git push -u origin main
Branch Workflow:
git checkout -b develop
git push origin develop
git checkout -b feature-enhancement
git add .
git commit -m "Added minor enhancement feature"
git push origin feature-enhancement
git checkout develop
git merge feature-enhancement
git push origin develop
git checkout -b release-v1
git push origin release-v1
git checkout main
git merge release-v1
git push origin main
git checkout develop
git merge release-v1
git push origin develop
Deletion(optional)
git branch -d feature-enhancement
git branch -d release-v1
Tag release
git tag v1.0
git push origin v1.0
''''''''''''''''
Application development
Git version control
GitHub hosting
Feature branch implementation
Develop branch workflow
Release branch management
Merge to main and develop
Version tagging.
